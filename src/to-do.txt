changes to be made:

all data needed from set to be passed as parameter - no hardcoding

must allow for prediction column and columns used to be passed as parameter

handle missing values -if prediction column missing remove row

number of neighbours to be used

optional weighting algorithm

use headings to create query